{
  "name": "reptileshub-landing",
  "version": "1.0.0",
  "description": "Beautiful landing page for ReptilesHub",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp public/.nojekyll dist/",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "vue": "^3.3.4",
    "vue-router": "^4.6.3"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.3.4",
    "gh-pages": "^6.1.0",
    "vite": "^4.4.9"
  }
}
